# Three-Layer System (C# .NET WinForms)

A **C# .NET 8** application with **Windows Forms** implementing the Three-Layer Architecture.  
The project focuses on full separation between layers, extensive use of Design Patterns, and data management using LINQ and XML files.

---

## 🚀 How to Run
1. Open the project in **Visual Studio**.
2. Build the solution: `Build > Build Solution` (or `Ctrl+Shift+B`).
3. Run the application: `Debug > Start Debugging` (or `F5`).
4. The main window will open – interact with the forms according to the implemented features.

---

## 🔧 Technologies & Tools
- **C# .NET 8** – Desktop application.
- **Windows Forms** – Graphical User Interface (GUI).
- **OOP & Design Patterns** – Use of Singleton, Factory, and other patterns.
- **Three-Layer Architecture** – Separation between Presentation, Business Logic, and Data Access.
- **LINQ & XML** – Data management and queries.

---

## ✨ Key Features
- Clear and modular separation of code into three layers.
- Implementation of Design Patterns for efficient instance and logic management.
- Reading and writing data using XML files.
- Interactive user interface with Windows Forms.

---

## 📌 Future Improvements
- Connect to an SQL database for persistent data storage.
- Expand the functionality of the GUI.
- Add Unit Tests for the business logic layers.
